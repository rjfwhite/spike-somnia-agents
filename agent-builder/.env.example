# IPFS Pinata credentials (for uploading containers and metadata)
PINATA_API_KEY=your_api_key
PINATA_SECRET_KEY=your_secret_key

# Optional: Custom IPFS API URL (for local node)
# IPFS_API_URL=http://localhost:5001
